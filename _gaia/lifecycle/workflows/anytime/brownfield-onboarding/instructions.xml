<workflow name="brownfield-onboarding">
<critical>
  <mandate>Document existing state before proposing changes</mandate>
  <mandate>Stories should only cover gaps, not re-implement existing features</mandate>
  <mandate>All diagrams must use Mermaid syntax — no ASCII art, no text descriptions of diagrams</mandate>
  <mandate>All API documentation must use Swagger/OpenAPI format</mandate>
  <mandate>Limit flow diagrams to 3-5 key flows to avoid output bloat</mandate>
</critical>

<step n="1" title="Deep Project Discovery">
  <action>Invoke document-project workflow to scan codebase</action>
  <action>Capture: tech stack, architecture, existing features, code quality, conventions</action>
  <action>API Detection: scan for route definitions, controllers, OpenAPI/Swagger specs. Set {has_apis} flag (true/false)</action>
  <action>Event Detection: scan for Kafka, RabbitMQ, SNS-SQS, Redis pub-sub, NATS, or other messaging patterns. Set {has_events} flag (true/false)</action>
  <action>Dependency Detection: scan for outbound HTTP clients, SDKs, service URLs, database connections. Set {has_external_deps} flag (true/false)</action>
  <action>Frontend Detection: scan for React, Angular, Vue, Flutter, SwiftUI, UI frameworks, CSS/styling. Set {has_frontend} flag (true/false)</action>
  <action>Testing infrastructure: identify test framework, coverage config, test count, test patterns</action>
  <action>CI/CD: identify GitHub Actions, Jenkins, Docker, Terraform, or other pipeline files</action>
  <action>Generate brownfield assessment: read {installed_path}/../../templates/brownfield-assessment-template.md — capture component inventory, technical debt, migration constraints, coexistence strategy, and adoption path. Output to {planning_artifacts}/brownfield-assessment.md</action>
  <template-output file="{planning_artifacts}/project-documentation.md">
    Generate enhanced project documentation following the project documentation conventions. Include all standard sections plus: detected capability flags (has_apis, has_events, has_external_deps, has_frontend), testing infrastructure summary, and CI/CD pipeline summary.
  </template-output>
</step>

<step n="2" title="Parallel Documentation Subagents">
  <action>Spawn subagents in parallel using the Agent tool with multiple calls in a single message. Only spawn subagents for detected capabilities.</action>
  <action>If {has_apis}: spawn subagent — "Scan the codebase at {project-root} for API routes, controllers, and OpenAPI/Swagger specs. Read {installed_path}/../../templates/api-documentation-template.md for the output format. Check if an OpenAPI spec file already exists — if so, validate it against actual routes; if not, generate an OpenAPI 3.x spec from code. Document all endpoints (method, path, handler, auth, parameters, request/response schemas, error formats). Include a Mermaid API flow diagram. List undocumented endpoints as gaps. All API docs must use Swagger/OpenAPI format. Output to {planning_artifacts}/api-documentation.md"</action>
  <action>If {has_frontend}: spawn subagent — "Scan the codebase at {project-root} for UI frameworks, components, design patterns, and styling. Read {installed_path}/../../templates/ux-design-assessment-template.md for the output format. Document existing UI patterns, navigation structure (Mermaid sitemap), interaction patterns, accessibility (WCAG compliance, ARIA, keyboard nav). Identify UX gaps — propose improvements for gaps only, not redesign of working patterns. Output to {planning_artifacts}/ux-design.md"</action>
  <action>If {has_events}: spawn subagent — "Scan the codebase at {project-root} for messaging patterns (Kafka, RabbitMQ, SNS-SQS, Redis pub-sub, NATS). Read {installed_path}/../../templates/event-catalog-template.md for the output format. Document messaging infrastructure, produced/consumed events with schemas, external events, delivery guarantees (retry, DLQ, idempotency). Include Mermaid event flow diagrams for 2-3 key flows. Output to {planning_artifacts}/event-catalog.md"</action>
  <action>Spawn subagent (always) — "Scan the codebase at {project-root} for external dependencies. Read {installed_path}/../../templates/dependency-map-template.md for the output format. Document external service dependencies (APIs consumed, auth, health checks), infrastructure dependencies (databases, caches, queues), key library dependencies (ORM, auth lib — check version currency and CVE risk). Create Mermaid dependency graph. Document contracts, SLAs, and fallback strategies. Identify dependency risks. Output to {planning_artifacts}/dependency-map.md. Then load {project-root}/_gaia/core/tasks/review-dependency-audit.xml, read its entire contents, and follow the flow steps EXACTLY to generate a dependency audit report."</action>
  <action>Wait for all subagents to complete. Verify each expected output file exists: dependency-map.md (always), api-documentation.md (if has_apis), ux-design.md (if has_frontend), event-catalog.md (if has_events), dependency-audit-{date}.md (always).</action>
</step>

<step n="3" title="NFR Assessment &amp; Performance Test Plan">
  <action>Spawn a subagent — "Analyze the codebase at {project-root} for non-functional requirements. Read {installed_path}/../../templates/nfr-assessment-template.md for the output format. Assess: code quality (linting, complexity, duplication), security posture (dependency vulnerabilities, secrets handling, auth quality — load security-basics skill section vulnerability-scanning if needed), performance (bundle size if frontend, query patterns, caching, resource management), accessibility (ARIA, semantic HTML, keyboard nav if frontend), test coverage (framework, count, coverage %, untested areas, quality), CI/CD (build pipeline, deploy strategy, environments, IaC). Create NFR Baseline Summary Table with measured values (not placeholders). Output to {test_artifacts}/nfr-assessment.md. Then create a performance test plan: load {project-root}/_gaia/testing/knowledge/performance/k6-patterns.md for load testing patterns. If {has_frontend}: also load {project-root}/_gaia/testing/knowledge/performance/lighthouse-ci.md. Define performance budgets (P50/P95/P99) based on NFR baselines, load test scenarios (gradual, spike, soak), backend profiling targets (slow queries, N+1, connection pools), CI performance gates. If {has_frontend}: define Core Web Vitals targets (LCP under 2.5s, INP under 200ms, CLS under 0.1). Output to {test_artifacts}/performance-test-plan-{date}.md"</action>
  <action>When subagent returns: verify both {test_artifacts}/nfr-assessment.md and {test_artifacts}/performance-test-plan-{date}.md exist.</action>
</step>

<step n="4" title="Create PRD for Gaps">
  <action>Read the PRD template from {installed_path}/../../templates/prd-template.md</action>
  <action>Read upstream artifacts to inform gap analysis:</action>
  <action>— nfr-assessment.md → NFR section gets real "Current Baseline" and "Target" columns</action>
  <action>— api-documentation.md (if exists) → extract API gaps (undocumented endpoints, missing validation)</action>
  <action>— event-catalog.md (if exists) → extract messaging gaps (missing DLQ, missing schemas)</action>
  <action>— dependency-map.md → extract dependency risks (outdated versions, missing health checks)</action>
  <action>— dependency-audit-{date}.md → extract critical/high findings as gap requirements (CVEs, outdated packages, license conflicts)</action>
  <action>— ux-design.md (if exists) → extract UX gaps (accessibility violations, inconsistent patterns)</action>
  <action>Invoke create-prd logic in brownfield mode: fill every section with gap-focused content only</action>
  <action>Overview = existing project summary + what gaps this PRD addresses</action>
  <action>Goals = gap closure goals only. Non-Goals = existing features that will NOT be re-implemented</action>
  <action>User Stories = stories for gap features only, not existing functionality</action>
  <action>Functional Requirements = gap requirements organized by priority</action>
  <action>Non-Functional Requirements = NFR gaps with current baseline and target from {test_artifacts}/nfr-assessment.md</action>
  <action>If {planning_artifacts}/prd.md already exists: WARN user — "A PRD already exists. Continuing will overwrite it with brownfield gap content. Choose: (a) overwrite, (b) save as prd-brownfield-gaps.md instead." If user chooses (b), adjust template-output path accordingly.</action>
  <template-output file="{planning_artifacts}/prd.md">
    Generate a PRD following the standard template structure. Add "Mode: Brownfield — gaps only" to the header. All sections must follow the template format but contain only gap-focused content. NFR section must include "Current Baseline" and "Target" columns with real values from {test_artifacts}/nfr-assessment.md. Include a Priority Matrix section mapping each gap to priority/effort/impact.
  </template-output>
</step>

<step n="5" title="Adversarial Review &amp; PRD Refinement">
  <action>Spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/tasks/review-adversarial.xml. Read its entire contents. Target: {planning_artifacts}/prd.md. Follow the task instructions EXACTLY." This is required — findings will be incorporated back into the PRD.</action>
  <action>When subagent returns: verify adversarial-review-{date}.md exists in {planning_artifacts}/</action>
  <action>Read {planning_artifacts}/adversarial-review-{date}.md — extract critical and high severity findings</action>
  <action>For each critical/high finding: add as a new requirement or refine an existing requirement in the PRD</action>
  <action>Add a "## Review Findings Incorporated" section to the PRD listing each finding, its severity, and how it was addressed</action>
  <template-output file="{planning_artifacts}/prd.md">
    Update the PRD with adversarial review findings incorporated. Critical/high findings become new requirements or refine existing ones. Add "Review Findings Incorporated" section.
  </template-output>
</step>

<step n="6" title="Architecture">
  <action>Architecture is created using the Phase 3 workflow which auto-detects brownfield mode from the PRD "Mode: Brownfield" header set in step 4.</action>
  <action>Spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/engine/workflow.xml, then process {project-root}/_gaia/lifecycle/workflows/3-solutioning/create-architecture/workflow.yaml as workflow-config. Run in YOLO mode. Follow the workflow engine instructions EXACTLY." When subagent returns: verify {planning_artifacts}/architecture.md exists.</action>
  <action>Epics and stories require a test plan first. The remaining Phase 3 sequence (/gaia-test-design → /gaia-create-epics → /gaia-readiness-check) must be run after this workflow completes.</action>
</step>
<next-step command="/gaia-test-design">
  Run Phase 3 sequence: /gaia-test-design → /gaia-create-epics → /gaia-readiness-check. Start with /gaia-test-design to create the test plan required by create-epics.
</next-step>
</workflow>
