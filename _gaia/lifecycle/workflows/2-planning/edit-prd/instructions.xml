<workflow name="edit-prd">
<critical>
  <mandate>Preserve existing content not being changed</mandate>
  <mandate>Add version note documenting what changed and why</mandate>
  <mandate>Update "Review Findings Incorporated" section after adversarial review</mandate>
</critical>
<step n="1" title="Load PRD">
  <action>Read current {planning_artifacts}/prd.md</action>
  <action>Display current structure summary to user</action>
</step>
<step n="2" title="Identify Changes">
  <ask>What sections need to change?</ask>
  <ask>Why are these changes needed?</ask>
  <ask>Is this linked to a change request? If so, provide the CR ID.</ask>
  <action>Confirm scope of changes before proceeding</action>
</step>
<step n="3" title="Apply Edits">
  <action>Make requested changes while preserving unchanged content</action>
  <action>Validate consistency with remaining sections</action>
  <action>Add version note at top: date, changes made, reason, CR ID (if applicable)</action>
</step>
<step n="4" title="Save Updated PRD">
  <template-output file="{planning_artifacts}/prd.md">
    Save updated PRD with version note prepended
  </template-output>
</step>
<step n="5" title="Adversarial Review">
  <ask>The PRD has been updated. Would you like to run an adversarial review? This spawns a subagent in a separate context — recommended for significant changes. (yes / skip for minor edits)</ask>
  <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/tasks/review-adversarial.xml. Read its entire contents. Target: {planning_artifacts}/prd.md. Follow the task instructions EXACTLY."</action>
  <action>If skip: proceed to step 7 — mark "Review Findings Incorporated" as "Incremental edit — adversarial review skipped (minor change)"</action>
  <action>If ok: wait for user to confirm adversarial-review-{date}.md exists</action>
</step>
<step n="6" title="Incorporate Review Findings" optional="true" condition="adversarial_completed">
  <action>Read {planning_artifacts}/adversarial-review-{date}.md — extract critical and high severity findings</action>
  <action>For each critical/high finding: add or refine requirement in the PRD</action>
  <action>Update the "## Review Findings Incorporated" section — append new entries with amendment date</action>
  <template-output file="{planning_artifacts}/prd.md">
    Update the PRD with adversarial review findings incorporated. Append to existing "Review Findings Incorporated" section with amendment date.
  </template-output>
</step>
<step n="7" title="Architecture Cascade Check">
  <action>Read {planning_artifacts}/architecture.md section headers</action>
  <action>Compare PRD changes against architecture scope</action>
  <action>Classify cascade impact:</action>
  <action>  NONE: requirements-only changes — architecture unaffected</action>
  <action>  MINOR: architecture needs a section update — suggest manual review</action>
  <action>  SIGNIFICANT: new component/API/data model — recommend /gaia-adversarial on architecture.md, then /gaia-add-stories</action>
  <action>Report cascade assessment to user with recommended next command(s)</action>
</step>
</workflow>
