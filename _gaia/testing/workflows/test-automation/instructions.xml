<workflow name="test-automation">
<critical>
  <mandate>Identify coverage gaps before writing new tests</mandate>
  <mandate>Load unit testing knowledge for the project stack</mandate>
  <mandate>Story must be in review status before test automation</mandate>
  <mandate>Output must include machine-readable PASSED or FAILED verdict</mandate>
  <mandate>After updating the Review Gate, if all 6 rows are PASSED and story is in 'review', you MUST transition story to 'done'</mandate>
</critical>
<step n="1" title="Load Story and Code">
  <action>Read story file, identify files changed and acceptance criteria</action>
  <action>Read implemented code files</action>
</step>
<step n="2" title="Status Gate">
  <action>Verify story is in 'review' status</action>
  <check if="status != review">HALT: Story must be in review status before test automation</check>
</step>
<step n="3" title="Identify Coverage Gaps">
  <action>Analyze existing tests vs code paths</action>
</step>
<step n="4" title="Load Knowledge">
  <action>Load unit test patterns for the project's stack from {knowledge_path}/</action>
</step>
<step n="5" title="Generate Tests">
  <action>Write tests for uncovered code paths</action>
  <action>Follow AAA pattern and naming conventions</action>
</step>
<step n="6" title="Run and Validate">
  <action>Execute tests, verify they pass and cover intended paths</action>
  <action>If ALL tests pass: verdict is PASSED</action>
  <action>If ANY test fails: verdict is FAILED — list failing tests with reasons</action>
</step>
<step n="7" title="Generate Report">
  <action>Generate automation report with coverage gaps filled and test results</action>
  <template-output file="{test_artifacts}/automation-report.md">
    Generate automation report with coverage gaps filled, test results, and machine-readable verdict (PASSED/FAILED).
  </template-output>
</step>
<step n="8" title="Update Review Gate">
  <action>Read story file, locate Review Gate table</action>
  <action>If Review Gate table is missing: initialize it with EXACTLY 6 rows — Code Review (PENDING), QA Tests (PENDING), Security Review (PENDING), Test Automation (PENDING), Test Review (PENDING), Performance Review (PENDING). Do NOT add any other rows.</action>
  <action>If PASSED: update Test Automation row status to PASSED, link report file</action>
  <action>If FAILED: update Test Automation row status to FAILED, link report file, set story status to 'in-progress' in story file and sprint-status.yaml</action>
  <template-output file="{implementation_artifacts}/{story_key}.md">
    Update story file Review Gate table with test automation result and report link. If FAILED, also update story status to in-progress.
  </template-output>
</step>
<step n="9" title="Transition Story If Gate Passed">
  <critical>
    <mandate>You MUST execute this step even if the Review Gate was already fully passed before this workflow run. The purpose of this step is to ensure the story status matches the gate state.</mandate>
  </critical>
  <action>Re-read the story file now. Check two things: (1) the Review Gate table rows, (2) the current story status.</action>
  <action>The Review Gate table must have EXACTLY 6 rows: Code Review, QA Tests, Security Review, Test Automation, Test Review, Performance Review. Remove any other rows.</action>
  <action>If ALL 6 rows show PASSED AND the story status is still 'review': you MUST change the story status to 'done' in the story file AND in sprint-status.yaml. This is a REQUIRED write operation, not informational.</action>
  <action>If ALL 6 rows show PASSED AND the story status is already 'done': log "Story already done. No update needed."</action>
  <action>If any row is FAILED: log which reviews failed. Do not change story status.</action>
  <action>If any row is PENDING: log which reviews are still pending. Do not change story status.</action>
  <template-output file="{implementation_artifacts}/sprint-status.yaml">
    Read sprint-status.yaml. If all 6 Review Gate rows are PASSED and the story status is 'review', update the story status to 'done' and save the file.
  </template-output>
</step>
</workflow>
