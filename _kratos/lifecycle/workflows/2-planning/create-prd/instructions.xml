<workflow name="create-prd">
<critical>
  <mandate>Product brief must exist before starting</mandate>
  <mandate>Requirements must be discoverable, not guessed</mandate>
  <mandate>Each requirement must have testable acceptance criteria</mandate>
  <mandate>Every requirement must have a unique ID: FR-### (functional), NFR-### (non-functional)</mandate>
</critical>
<step n="1" title="Load Product Brief">
  <action>Read {planning_artifacts}/product-brief.md</action>
  <action>Extract vision, target users, problem statement, proposed solution</action>
  <check if="{planning_artifacts}/prd.md exists and contains 'Mode: Brownfield'">WARN: A brownfield PRD exists at {planning_artifacts}/prd.md. Continuing will overwrite it. Confirm with user before proceeding.</check>
</step>
<step n="2" title="User Interviews">
  <ask>Who are the primary user types?</ask>
  <ask>What are their top 3 needs?</ask>
  <ask>What frustrates them most about current solutions?</ask>
  <action>Structure responses into user need statements</action>
</step>
<step n="3" title="Functional Requirements">
  <action>List features organized by priority: Must-Have, Should-Have, Nice-to-Have</action>
  <action>Each feature needs: description, user story format, acceptance criteria</action>
  <action>Assign unique IDs: FR-001, FR-002, ... — IDs are sequential and never reused</action>
</step>
<step n="4" title="Non-Functional Requirements">
  <action>Define: performance targets, security requirements, accessibility standards</action>
  <action>Define: scalability needs, reliability targets, compliance requirements</action>
  <action>Assign unique IDs: NFR-001, NFR-002, ... — IDs are sequential and never reused</action>
</step>
<step n="5" title="User Journeys">
  <action>Map key user flows with happy path and error paths</action>
  <action>Include: entry point, steps, decision points, exit conditions</action>
</step>
<step n="6" title="Data Requirements">
  <action>Identify data stored, processed, and exchanged</action>
  <action>Define data retention, privacy, and security policies</action>
</step>
<step n="7" title="Integration Requirements">
  <action>List external systems, APIs, and third-party services</action>
  <action>Define integration patterns and data exchange formats</action>
  <action>For each critical dependency: define failure mode, fallback behavior, and SLA expectations</action>
</step>
<step n="8" title="Out of Scope">
  <action>List features, integrations, and use cases explicitly excluded from this release</action>
  <action>For each exclusion: state what is excluded and why (deferred, not needed, separate product)</action>
</step>
<step n="9" title="Constraints and Assumptions">
  <action>Document technical, budget, timeline, and team constraints</action>
  <action>List all assumptions that requirements depend on</action>
</step>
<step n="10" title="Success Criteria">
  <action>Define measurable acceptance criteria for each feature</action>
  <action>Define overall product success metrics</action>
</step>
<step n="11" title="Generate Output">
  <template-output file="{planning_artifacts}/prd.md">
    Generate PRD with all sections: overview, user personas, functional requirements (prioritized with FR-### IDs), non-functional requirements (with NFR-### IDs), user journeys, data requirements, integrations, out of scope, constraints, assumptions, success criteria. Include a Requirements Summary Table listing all FR and NFR IDs with their description, priority, and status.
  </template-output>
</step>
<step n="12" title="Adversarial Review">
  <action>Spawn a subagent using the Agent tool: "Load {project-root}/_kratos/core/tasks/review-adversarial.xml. Read its entire contents. Target: {planning_artifacts}/prd.md. Follow the task instructions EXACTLY." This is required — findings will be incorporated back into the PRD.</action>
  <action>When subagent returns: verify adversarial-review-{date}.md exists in {planning_artifacts}/</action>
</step>
<step n="13" title="Incorporate Adversarial Findings">
  <action>Read {planning_artifacts}/adversarial-review-{date}.md — extract critical and high severity findings</action>
  <action>For each critical/high finding: add as a new requirement or refine an existing requirement in the PRD</action>
  <action>Add a "## Review Findings Incorporated" section to the PRD listing each finding, its severity, and how it was addressed (new requirement added / existing requirement refined / acknowledged as risk)</action>
  <template-output file="{planning_artifacts}/prd.md">
    Update the PRD with adversarial review findings incorporated. Critical/high findings become new requirements or refine existing ones. Add "Review Findings Incorporated" section.
  </template-output>
</step>
</workflow>
