name: sprint-planning
description: 'Generate sprint plan from epics and stories'
module: lifecycle
agent: sm
config_resolved: "{project-root}/_kratos/lifecycle/.resolved/sprint-planning.yaml"
config_source: "{project-root}/_kratos/lifecycle/config.yaml"
installed_path: "{project-root}/_kratos/lifecycle/workflows/4-implementation/sprint-planning"
instructions: "{installed_path}/instructions.xml"
validation: "{installed_path}/checklist.md"
input_file_patterns:
  epics:
    whole: "{planning_artifacts}/epics-and-stories.md"
    load_strategy: "FULL_LOAD"
quality_gates:
  pre_start:
    - check: "test_plan_exists"
      on_fail: "WARN: Test plan not found at {test_artifacts}/test-plan.md. Sprint risk assessment will be incomplete. Consider running /kratos-test-design."
tracking_system: "file-system"
status_file: "{implementation_artifacts}/sprint-status.yaml"
story_statuses: [backlog, validating, ready-for-dev, in-progress, blocked, review, done]
on_error:
  missing_file: "ask_user"
  unresolved_variable: "halt"

output:
  primary: "{implementation_artifacts}/sprint-{sprint_id}-plan.md"
