<workflow name="sprint-planning">
<critical>
  <mandate>Use sprint state machine: backlog, validating, ready-for-dev, in-progress, blocked, review, done</mandate>
  <mandate>Support multi-sprint via sprint_id tracking</mandate>
</critical>
<step n="1" title="Load Epics and Stories">
  <action>Read {planning_artifacts}/epics-and-stories.md</action>
  <action>Parse all stories with their priorities, sizes, dependencies</action>
</step>
<step n="2" title="Optional: Memory Hygiene Check" optional="true">
  <action>Check if any sidecar directories under _kratos/_memory/*-sidecar/ contain content beyond their template header</action>
  <check if="any sidecars have content">
    <ask>Agent memory sidecars contain decisions from previous sessions. Would you like to run memory hygiene? This spawns a subagent in a separate context. Recommended if architecture or requirement changes occurred since last sprint. (yes / skip)</ask>
    <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_kratos/core/engine/workflow.xml, then process {project-root}/_kratos/lifecycle/workflows/anytime/memory-hygiene/workflow.yaml as workflow-config. Run in YOLO mode. Follow the workflow engine instructions EXACTLY."</action>
    <action>If skip: memory hygiene can be run anytime with /kratos-memory-hygiene</action>
  </check>
  <check if="no sidecars have content">Skip — no sidecar data to review.</check>
</step>
<step n="3" title="Sprint Scoping">
  <ask>Sprint duration (1 week / 2 weeks / custom)?</ask>
  <ask>Team velocity estimate (story points)?</ask>
  <ask>Sprint number (for multi-sprint tracking)?</ask>
</step>
<step n="4" title="Story Selection">
  <action>Select stories for this sprint based on priority and dependencies</action>
  <action>Ensure total size fits within velocity estimate</action>
  <action>Respect dependency ordering — blocked stories cannot be selected without their blockers</action>
  <action>If {test_artifacts}/test-plan.md exists: apply risk levels to story selection — buffer 20% for high-risk stories in velocity estimate</action>
  <action>Flag any high-risk story that does not have a corresponding {test_artifacts}/atdd-{story_key}.md file — add a sprint risk note</action>
</step>
<step n="5" title="Optional: Mobile Testing">
  <ask>Would you like to create a mobile test plan? This spawns a subagent in a separate context. Recommended for mobile or responsive applications. (yes / skip)</ask>
  <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_kratos/core/engine/workflow.xml, then process {project-root}/_kratos/testing/workflows/mobile-testing/workflow.yaml as workflow-config. Run in YOLO mode. Follow the workflow engine instructions EXACTLY."</action>
  <action>If skip: mobile testing can be set up anytime later with /kratos-mobile-testing</action>
</step>
<step n="6" title="Sprint Plan Generation">
  <action>Create sprint plan with story assignments and execution order</action>
  <action>Order by dependency resolution + priority</action>
</step>
<step n="7" title="Status Initialization">
  <action>Set all selected stories to 'backlog' status</action>
  <action>Generate sprint-status.yaml with format:
    sprint_id: "sprint-{N}"
    started: "{date}"
    stories:
      - key: "{story-key}"
        status: "backlog"
        assignee: null
        blocked_by: null
        updated: "{date}"
  </action>
</step>
<step n="8" title="Generate Output">
  <template-output file="{implementation_artifacts}/sprint-{sprint_id}-plan.md">
    Generate sprint plan document with: sprint goals, selected stories (ordered), velocity target, risk assessment, and a Testing Readiness section listing high-risk stories, ATDD file status for each, and any missing test artifacts.
  </template-output>
  <action>Write sprint-status.yaml to {implementation_artifacts}/sprint-status.yaml</action>
</step>
</workflow>
