<workflow name="validate-story">
<critical>
  <mandate>Append validation results to the story file</mandate>
  <mandate>Read the story template from {project-root}/_kratos/lifecycle/templates/story-template.md for reference</mandate>
</critical>
<step n="1" title="Load Story">
  <ask>Which story to validate? (provide story key, e.g., 1.2)</ask>
  <action>Find and read the story file matching {story_key}-*.md in {implementation_artifacts}/</action>
</step>
<step n="2" title="Header Metadata">
  <action>Verify header metadata is present and populated: Epic, Sprint, Date, Author, Status, Traces to</action>
</step>
<step n="3" title="Completeness">
  <action>Verify all sections present: User Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes, Technical Notes, Dependencies, Test Scenarios, Project Structure Notes, References, Dev Agent Record, Estimate, Definition of Done</action>
</step>
<step n="4" title="Clarity">
  <action>Check each AC is testable and unambiguous (Given/When/Then format)</action>
  <action>Verify subtasks are actionable and linked to AC numbers</action>
  <action>Verify Definition of Done items are specific and measurable</action>
</step>
<step n="5" title="Dependencies">
  <action>Verify all declared dependencies exist and are resolvable</action>
</step>
<step n="6" title="Generate Validation Report">
  <action>Append validation result at the END of the story file, after a horizontal rule (---) separator</action>
  <template-output file="{implementation_artifacts}/{story_key}-{story_title_slug}.md">
    Append a section titled "## Validation Report" at the very end of the story file, preceded by a --- separator. Include: date, overall PASS/FAIL verdict, and per-section findings (PASS or FAIL with specific issue description). This report will be read by /kratos-fix-story to resolve issues.
  </template-output>
</step>
<step n="7" title="Update Story Status">
  <action>If overall verdict is PASS: update story status to 'ready-for-dev' in the story file header and in {implementation_artifacts}/sprint-status.yaml</action>
  <action>If overall verdict is FAIL: keep story status as 'validating' — instruct user to run /kratos-fix-story then re-run /kratos-validate-story</action>
  <template-output file="{implementation_artifacts}/sprint-status.yaml">
    Update sprint-status.yaml with the story status: ready-for-dev (if PASS) or validating (if FAIL).
  </template-output>
</step>
</workflow>
