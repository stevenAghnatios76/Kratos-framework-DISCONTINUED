<workflow name="tech-debt-review">
<critical>
  <mandate>Read ONLY frontmatter + Findings section from story files — never load full content (token budget protection)</mandate>
  <mandate>Every debt item must be classified, scored, and aged — none may be left unprocessed</mandate>
  <mandate>Dashboard is a living document — overwrite on each run, preserve trend data from previous version</mandate>
</critical>
<step n="1" title="Scan Debt Sources">
  <action>If {implementation_artifacts}/tech-debt-dashboard.md exists: load it for trend comparison (previous totals, previous items list)</action>
  <action>Read {implementation_artifacts}/sprint-status.yaml — identify current sprint_id and all stories with status 'backlog'</action>
  <action>Scan story files in {implementation_artifacts}/ — for each .md file:</action>
  <action>  1. Read YAML frontmatter (stop at closing ---) to get story key, status, sprint_id, type</action>
  <action>  2. Scan for ## Findings section — collect rows where Type = 'tech-debt'</action>
  <action>  3. Also collect findings of Type = 'bug' with Severity = 'medium' or 'low' that are NOT marked [TRIAGED] or [DISMISSED]</action>
  <action>Separate items into two pools: (a) untriaged findings still in story files, (b) triaged debt stories already in backlog</action>
  <action>If untriaged findings exist: note count and recommend running /kratos-triage-findings first</action>
  <action>If no debt items found at all: inform user "No technical debt detected" and stop</action>
</step>
<step n="2" title="Classify Debt">
  <action>Categorize each debt item into exactly one category:</action>
  <action>  DESIGN — architectural shortcuts, missing abstractions, tight coupling, violation of design patterns</action>
  <action>  CODE — code smells, duplication, excessive complexity, naming inconsistencies, SOLID violations</action>
  <action>  TEST — missing tests, flaky tests, low coverage areas, manual-only testing</action>
  <action>  INFRASTRUCTURE — CI/CD gaps, missing monitoring, manual deployment steps, environment drift</action>
  <action>Tag each item with its source: 'finding' (from dev-story), 'triage' (backlog story from triage-findings), 'manual' (user-reported)</action>
  <action>Present classification to user in a summary table grouped by category</action>
</step>
<step n="3" title="Score and Prioritize">
  <action>For each debt item, assess three dimensions (1-5 scale):</action>
  <action>  Impact — How much does this affect development velocity, reliability, or user experience? (1=minimal, 5=severe)</action>
  <action>  Effort — How much work to resolve? (1=trivial fix, 2=few hours, 3=1 story, 4=multi-story, 5=epic-sized)</action>
  <action>  Risk of Inaction — What happens if ignored for 3 more sprints? (1=nothing, 3=slows team, 5=production incident)</action>
  <action>Calculate Debt Score = (Impact + Risk of Inaction) - Effort</action>
  <action>Map to priority: Score >= 7 = FIX NOW, 4-6 = PLAN NEXT, 1-3 = TRACK</action>
  <action>Sort all items by Debt Score descending</action>
  <ask>Review the top 5 scored items — confirm or adjust the scores</ask>
</step>
<step n="4" title="Calculate Aging">
  <action>For each debt item, calculate age in sprints:</action>
  <action>  - Triaged backlog stories: compare story creation sprint to current sprint_id</action>
  <action>  - Untriaged findings: compare source story sprint_id to current sprint_id</action>
  <action>  - If sprint_id not available: use file modification date as fallback</action>
  <action>Apply SLA thresholds and escalate where needed:</action>
  <action>  FIX NOW items older than 1 sprint → flag as OVERDUE</action>
  <action>  PLAN NEXT items older than 3 sprints → auto-escalate to FIX NOW</action>
  <action>  TRACK items older than 5 sprints → flag for relevance review (may be obsolete)</action>
  <action>Build aging distribution: count items per age bucket (current sprint, 1-2 sprints, 3-4 sprints, 5+ sprints)</action>
</step>
<step n="5" title="Generate Dashboard">
  <action>Load template from {project-root}/_kratos/lifecycle/templates/tech-debt-dashboard-template.md</action>
  <action>Fill summary metrics: total count, FIX NOW / PLAN NEXT / TRACK counts, overdue count, debt ratio</action>
  <action>Fill category table: count and oldest item age per category</action>
  <action>Fill aging histogram: ASCII bar chart showing items per age bucket</action>
  <action>Fill top 10 table: item description, category, impact, effort, risk, score, age, priority, source, recommended action</action>
  <action>Fill overdue alerts: list all OVERDUE and auto-escalated items with source and age</action>
  <action>Fill trend section: compare current totals to previous dashboard (if exists), compute deltas</action>
  <action>  If no previous dashboard: show "First review — no prior data" in trend section</action>
  <template-output file="{implementation_artifacts}/tech-debt-dashboard.md">
    Generate tech debt dashboard from template with all metrics, rankings, aging alerts, and trend data.
  </template-output>
</step>
<step n="6" title="Recommend Actions">
  <action>Based on dashboard findings, recommend specific next steps:</action>
  <action>  If any FIX NOW + OVERDUE items exist: "Run /kratos-correct-course to inject these into the current sprint"</action>
  <action>  If debt ratio > 30%: "Consider dedicating 20% of next sprint capacity to debt reduction"</action>
  <action>  If TEST debt dominates: "Run /kratos-test-automate to address test coverage gaps"</action>
  <action>  If DESIGN debt dominates: "Consider /kratos-change-request for architectural refactoring"</action>
  <action>  If INFRASTRUCTURE debt dominates: "Run /kratos-ci-setup to close CI/CD gaps"</action>
  <action>  If items were auto-escalated: "Review escalated items — aging has upgraded their priority"</action>
  <action>Present the specific /kratos-* command chain for each top-priority item</action>
</step>
</workflow>
